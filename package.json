{
  "name": "kweb-exemption-task",
  "version": "1.0.0",
  "description": "!",
  "main": "index.js",
  "scripts": {
    "start": "npm-run-all --parallel start:**",
    "start:server": "cd backend && nodemon server",
    "start:client": "cd frontend && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "heochan",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^9.9.1",
    "eslint-config-prettier": "^9.1.0",
    "nodemon": "^3.1.4",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.3.3"
  }
}
